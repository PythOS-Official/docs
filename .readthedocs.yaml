# .readthedocs.yaml (v2)
version: 2

build:
  os: ubuntu-22.04         # Required: choose one valid Ubuntu image
  tools:
    python: "3.10"         # Required: specify at least one tool

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: docs/requirements.txt
